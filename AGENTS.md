# Инструкции для агентов

- Дев-сервер (`npm start`, http://localhost:8080) по умолчанию уже запущен юзером. Не запускать и не гасить его. Запускать самому только если http://localhost:8080 не отвечает. Не предлагать его запустить и не спрашивать об этом.

## Типографика постов

Эталон английской типографики — `posts/en/2013-10-11 – Links, please/index.md`.

- **EN:** апостроф типографский `’` (`it’s`, `won’t`, `website’s`), кавычки `“ ”`, тире en dash (U+2013) с пробелами ` – `, многоточие тремя точками `...`, сокращение `e.&nbsp;g.` с неразрывным пробелом, клавиши через `<kbd>`.
- **RU:** тире em dash (U+2014) с пробелами ` — `, кавычки «ёлочки».
