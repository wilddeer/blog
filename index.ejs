---
layout: layouts/content
title: Утром доделаю
lang: ru
---

<p>
    Это бложек Олега Корсунского. Тут всякое нытье и че-то про разработку.
</p>

<ul>
    <% collections.post.forEach(post => { %>
        <% if (post.data.lang === 'ru') { %>
            <li>
                <a href="<%= post.url %>">
                    <%= post.data.title %>
                </a>
                —
                <%= post.data.date %>
            </li>
        <% } %>
    <% }); %>
</ul>
